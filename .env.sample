# Environment variables for the Post Service

# Port for the Express server
PORT=3000

# MongoDB credentials for authentication
# These should match the values set in the MongoDB service in docker-compose.yml
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=password

# The MongoDB connection URI used by Mongoose
# Using the service name 'mongo' defined in docker-compose.yml
MONGODB_URI=mongodb://mongo:27017/postdb